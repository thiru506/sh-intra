<?xml version="1.0" encoding="UTF-8"?>
<site-conf xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="http://ofbiz.apache.org/dtds/site-conf.xsd">
    <!-- The controller elements that are common to all OFBiz components
         can be found in the following xml file. A component can override the
         elements found in the common-controller.xml file. -->
    <include location="component://common/webcommon/WEB-INF/common-controller.xml"/>

    <description>ShopsMint Component Site Configuration File</description>

	<handler name="jsp" type="view" class="org.ofbiz.webapp.view.JspViewHandler"/>
    <!-- Events to run on every request before security (chains exempt) -->
    <!--
    <preprocessor>
    </preprocessor>
    -->
    <!-- Events to run on every request after all other processing (chains exempt) -->
    <!--
    <postprocessor>
        <event name="test" type="java" path="org.ofbiz.webapp.event.TestEvent" invoke="test"/>
    </postprocessor>
    -->

    <!-- Request Mappings -->
    <request-map uri="main"><security https="true" auth="true"/><response name="success" type="view" value="main"/></request-map>
    <request-map uri="store">
	    <security https="true" auth="false"/>
	    <response name="success" type="view" value="store"/>
    </request-map>
    <request-map uri="products">
	    <security https="true" auth="false"/>
	    <response name="success" type="view" value="products"/>
    </request-map>

    <request-map uri="merchantrequest">
       <event type="service"  invoke="getMerchants"></event>
	    <response name="success" type="request" value="json"/>
    </request-map>
    <!-- View Mappings -->
    <view-map name="main" type="screen" page="component://shopsmint/widget/ShopsMintScreens.xml#main"/>
    <view-map name="store" type="screen" page="component://shopsmint/widget/ShopsMintScreens.xml#store"/>
    <view-map name="products" type="screen" page="component://shopsmint/widget/ShopsMintScreens.xml#products"/>
</site-conf>